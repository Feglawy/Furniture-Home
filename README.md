# Furniture-Home Backend

## Environment Setup
There is a file called `.env-example`
```terminal
cp .env-example .env
```
This will copy the `.env-example` content's and add a `.env` file  
make sure to add ur configs there

